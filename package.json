{
  "name": "com.yealink.uc86-camera-control",
  "version": "1.0.0",
  "description": "Elgato Stream Deck plugin to control Yealink UC86 camera",
  "author": "Your Name",
  "license": "MIT",
  "main": "dist/plugin/index.js",
  "scripts": {
    "build": "npm run clean && npm run build:plugin && npm run build:pi",
    "build:plugin": "tsc -p tsconfig.plugin.json",
    "build:pi": "tsc -p tsconfig.pi.json",
    "clean": "rm -rf dist",
    "watch": "tsc -p tsconfig.plugin.json --watch",
    "dev": "npm run build && npm run watch"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "ws": "^8.14.0"
  }
}
